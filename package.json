{
  "name": "smashdem",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "smoke": "vite build --ssr tools/headlessBattle.ts --outDir dist-tools --logLevel warn && node dist-tools/headlessBattle.js",
    "smoke:run": "vite build --ssr tools/runFlow.ts --outDir dist-tools --logLevel warn && node dist-tools/runFlow.js",
    "smoke:tiers": "vite build --ssr tools/botTiers.ts --outDir dist-tools --logLevel warn && node dist-tools/botTiers.js",
    "smoke:mirror": "vite build --ssr tools/botMirror.ts --outDir dist-tools --logLevel warn && node dist-tools/botMirror.js",
    "smoke:reroll": "vite build --ssr tools/rerollFlow.ts --outDir dist-tools --logLevel warn && node dist-tools/rerollFlow.js",
    "smoke:pvp-protocol": "vite build --ssr tools/pvpProtocol.ts --outDir dist-tools --logLevel warn && node dist-tools/pvpProtocol.js",
    "smoke:online-battle": "vite build --ssr tools/onlineBattle.ts --outDir dist-tools --logLevel warn && node dist-tools/onlineBattle.js"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
