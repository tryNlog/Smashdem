# AGENTS.md — Smashdem (Claude ↔ Codex 릴레이 협업)

이 파일은 Codex CLI(및 임의 AI 코딩 에이전트)가 리포 루트에서 읽는 지침서다. Claude Code도 참고하되, Claude의 최상위 규칙은 사용자 글로벌 CLAUDE.md에서 온다.

## 0. 이 협업의 형태 — 세션 제한 릴레이 (바통 이어달리기)
- Claude Code가 세션 사용량 제한에 막히면 Codex가 이어받아 작업한다. **Claude 제한이 풀리는 시각**에 맞춰 Codex가 작업을 매듭짓고 Claude에 인계하고, 다시 Claude가 이어받는다.
- **동시 병렬이 아니다. 한 번에 한 에이전트만** 같은 브랜치(s2-run)에서 작업한다 → 충돌 없음.
- 바통은 `docs/RELAY.md`. 작업 시작 전 반드시 읽고, 손 떼기 전 반드시 갱신한다.
- 물리적 실행 한계: Claude는 제한에 막히는 순간 스스로 Codex를 실행할 수 없다(그냥 멈춤). 그래서 "누가 누구를 켜는가"는 자동화가 아니라 RELAY.md를 항상 최신으로 유지하는 것으로 푼다. 각자 자신의 복귀 가능 시각을 RELAY.md에 남긴다.

## 1. 프로젝트 한 줄
NHN NAN 2026 해커톤 사전과제. 로그라이크 팽이(탑블레이드) 배틀 웹게임 "Smashdem". 바닐라 TypeScript + Canvas 2D + Vite. 최종 콘텐츠는 실시간 온라인 PvP.

## 2. 절대 원칙 (2대 원칙 — PM 확정, 위반 시 자가 반려)
1. **자기긍정 금지**: "완벽/훌륭/최적/통과/완료/성공적" 등 자기 작업 자화자찬 금지. 완성·품질 판정은 PM 몫이다. 산출물 상태는 "작성됨(PM 판정 필요)".
2. **근거 제시 의무**: 모든 주장에 PM이 확인 가능한 근거(파일:라인, 측정값+측정방법). 근거를 즉시 못 대면 본문에 `[UNSUPPORTED]` 라벨을 붙인다.

## 3. 하드 제약 (깨면 프로젝트가 부서진다)
- **push 금지.** remote 조작은 PM만 한다. 로컬 커밋까지만.
- **결정론(src/game/) 신성불가침.** `src/game`은 순수 시뮬레이션이다: DOM·Date.now()·Math.random() 금지, 시드 PRNG·고정 timestep(1/60)만. 렌더/이펙트/오디오는 `src/render`. 이 계층 분리가 온라인 PvP 동기화의 전제다. `src/game` 수정 시 반드시 `npm run smoke:run`으로 "동일 시드 재현 N/N 일치"를 확인한 뒤 커밋한다.
- **밸런스는 종결(CLOSED).** 헤드리스 측정 ①~④ + PM 결정(D-2 "학살 보상 유지")으로 닫혔다. 밸런스 수치 재개, 미러봇(코드상 mirrorBot*, DORMANT 상태) 활성화 금지. 재개는 PM 지시가 있을 때만.
- **커밋은 컴파일되는 조각 단위로 즉시.** 에이전트 중단·세션 제한·인계 대비 유일한 안전망이다. 어중간한 미완성 코드를 남기고 손 떼지 마라.
- **02_게임설계.md는 통째로 읽지 마라(초대형).** 필요한 섹션만 열어라.

## 4. 명령
- 개발 서버: `npm run dev`
- 빌드: `npm run build` (tsc --noEmit + vite build)
- 결정론/밸런스 스모크: `npm run smoke:run`, `npm run smoke`, `npm run smoke:tiers`, `npm run smoke:mirror`, `npm run smoke:reroll`
- 모바일 입력 스모크: `npm run smoke:touch-input`

## 5. 어디에 뭐가 있나
- 소스: `src/game`(순수 시뮬·결정론), `src/render`(렌더·이펙트·오디오), `src/app`(세션 오케스트레이션), `tools`(헤드리스 측정 하버스트)
- 설계·기록 문서 (리포 밖, `../` = NAN2026/): `00_허브.md`(세션 브리지), `02_게임설계.md`(초대형, 섹션만 열 것), `03_일정.md`(일정·PM 결정 로그), `05_플레이테스트.md`(플레이테스트 기록)
- 개발 로그: `docs/ai-log.md` — 제출 문서 #4(AI 활용 기술)의 소재. 작업할 때마다 (목표 → 실제 프롬프트/시도 → 결과 → 사람이 고친 것·이유) 기록.

## 6. 릴레이 프로토콜 (핵심)
### 작업 시작 시
1. `docs/RELAY.md`를 읽는다 — 현재 담당·진행 중 작업·다음 큐·상대가 남긴 인계.
2. `git log --oneline -5`, `git status`로 실제 코드 상태를 확인한다.
3. 진행 중 작업이 있으면 이어서, 없으면 다음 큐 최상단부터.
### 작업 중
- 조각마다 커밋. `docs/ai-log.md` 갱신.
### 손 떼기 전 (인계) — 반드시
1. 컴파일되는 상태로 커밋한다.
2. `docs/RELAY.md`를 갱신한다: 방금 한 것, 마지막 커밋 해시, 어디까지/어디서 멈췄나, 다음 할 것, **다음 담당(Claude/Codex)과 그 담당의 복귀 가능 시각**.
3. Codex가 손 뗄 때: RELAY.md의 "Claude 제한 해제 예정 시각"을 참고해 그 즈음 자연스러운 매듭에서 종료한다. Claude가 손 뗄 때(제한 임박): 하니스가 표시한 제한 해제 시각을 RELAY.md에 적고 Codex에 인계한다.

## 7. PM에게 물어야 할 때 (그 외엔 자율)
프로젝트 방향성 변경, 밸런스 재개, push, 되돌리기 힘든 결정. PM 부재 시엔 그 항목을 `[UNSUPPORTED]`/미결로 RELAY.md에 남기고 자율 가능한 다른 큐로 넘어간다.
